---
name: 720-script-writer
description: >-
  Converts content from a "תבנית פיתוח תוכן ליעד" Word document into PPTX slides for a
  720 / methodica training script — 7th-8th grade units across any subject (math, science,
  etc.), not limited to a single project. Builds question segments (one PowerPoint Section per
  question, all lettered sub-parts א/ב/ג under it) by cloning matching screen templates from the
  dedicated `תבנית לתסריט.pptx` template library — never from the working script itself, and
  never by fabricating shapes from scratch. Use when the user asks to build/generate/convert
  slides for a specific question or section from content into a 720 script ("תבנה שקפים לסעיף...",
  "המר את השלב הזה לתסריט", "תוסיף שאלה 2 לתסריט").
  Stops and asks the user (does not build blind) whenever the required interaction type has no
  matching template in the template library, or when the Word content is ambiguous about which
  mechanism to use (e.g. dropdown vs. drag-and-drop for "השלמה מתוך מחסן").
  Do NOT use for: reviewing/QA-ing an already-built script (that's 720-script-qa), generic PPTX
  editing unrelated to 720 content conversion, or question banks in other formats (v.md).
---

# 720-script-writer

ממיר תוכן מקובץ Word ("תבנית פיתוח תוכן ליעד") לשקפי PPTX בתסריט 720 קיים, לפי מוסכמות שאומתו
בפועל מול תיקוני משתמש (לא ניחוש). כללי לכל 720 — לא ספציפי לפרויקט אחד.

**עקרון-על: הסקיל משכפל תמיד מ-`תבנית לתסריט.pptx` (ספריית-אב ייעודית, לא תלוית-פרויקט), ולעולם
לא בונה צורות מאפס ולא משתמש בתסריט הנבנה עצמו כמקור-דוגמה.** אם אין תבנית מתאימה — עוצרים ושואלים.

## קבצי רקע (לקרוא לפני התחלת עבודה)
- `references/word-doc-schema.md` — איך לפרש את טבלת התוכן ב-Word ולפרק שאלה לרכיביה.
- `references/section-structure.md` — מקטע = PowerPoint Section; מוסכמת שמות; מנגנון XML.
- `references/template-catalog.md` — קטלוג מלא של תבניות המסך + הכללים הגורפים שחלים על כולן.
- `references/content-style-rules.md` — כללי ניסוח/עיצוב (אות סעיף, בולד+סגול, יישור משוב וכו').
- `references/feedback-bank.md` — 32 פתיחי-משוב שליליים לבחירה אקראית (לא ניסוח קבוע).
- `references/metacognition-bank.md` — שאלות פיגום מטה-קוגניטיביות, לשילוב לפי דרישה.
- `references/construction-checklist.md` — תקלות ידועות + בדיקות אימות חובה.
- `scripts/pptx_slide_ops.py` — מודול Python עם כל פונקציות ה-XML/שכפול הנדרשות (מיובא, לא נכתב מחדש).

## תהליך עבודה

### שלב 1 — קלט
דרושים שלושה קבצים:
1. **קובץ Word** — התוכן לבנייה (שאלה/סעיף ספציפי, או שלב שלם).
2. **קובץ PPTX יעד** — התסריט הנבנה, שאליו מוסיפים שקפים.
3. **`תבנית לתסריט.pptx`** — ספריית התבניות. אם לא סופק ולא נמצא בתיקיית הפרויקט — לשאול את המשתמש
   איפה הוא נמצא, לא להמשיך בלעדיו (הוא מקור-האמת היחיד לתבניות).

לוודא ששני קבצי ה-PPTX **פתוחים לקריאה/כתיבה** (לא נעולים ע"י PowerPoint) לפני עריכה.

### שלב 2 — פענוח תוכן
לפי `word-doc-schema.md`: לאתר את השלב הרלוונטי בטבלת ה-Word, לקרוא את כל תא "תוכן להשלמה" בשלמותו,
ולפרק לפי תוויות "שאלה N X:"/"רמז N X:"/"משוב N X:". לזהות את סוג הפריט מעמודת "סוג הפריט / שאלה".
**חילוץ אך ורק דרך `scripts/docx_extract.py`** (קורא את משוואות ה-OMML שחילוץ רגיל מפיל), והתוכן
— תרחיש, מסיחים, רמזים ובמיוחד משובים — **מועתק מילה-במילה, בלי לנסח מחדש ובלי לחשב מחדש**
(checklist פריט 13). תוכן לא-קריא/חסר ⇒ לעצור ולשאול.

### שלב 3 — איתור עוגן ותבנית
- **עוגן במצגת היעד**: למצוא היכן להכניס (סוף שאלה קודמת/סוף מקטע קודם), תוך זיהוי ה-Sections
  הקיימים (`pptx_slide_ops.list_sections`).
- **תבנית מתאימה**: לחפש ב-`template-catalog.md` לפי סוג האינטראקציה מה-Word. אם הניסוח ב-Word
  מעורפל לגבי המנגנון המדויק (למשל "השלמה מתוך מחסן" — dropdown? גרירה?) — **לשאול את המשתמש**,
  לא לנחש.
- **שער עצירה:** אם אין שום תבנית תואמת ב-`תבנית לתסריט.pptx` — לעצור, להציג למשתמש בדיוק מה
  חסר ומה כן קיים שקרוב, ולחכות להנחיה. לא לאלתר פתרון מאפס.
- **תרגול מול הקנייה:** לפני שבוחרים דפוס-בנייה, לזהות מאיזה שלב מגיע התוכן (מעמודת "שלב" בוורד,
  או משם-המקטע היעד). שלבי תרגול → תמיד דפוס-3-שקפים (למטה). שלב **"הקניה"** → **לא** ברירת מחדל
  אוטומטית ל-3-שקפים — ייתכן דפוס-שקף-מאוחד להדגמת-פתרון (ר' `section-structure.md`, "שני דפוסים:
  תרגול מול הקנייה"). אם לא ברור מהוורד איזה משני הדפוסים מתאים לתוכן ההקנייה — לעצור ולשאול.

### שלב 4 — בנייה
**דרך המלך: `pptx_slide_ops.build_question(src_prs, dest_prs, spec)`** — בנאי דקלרטיבי שמקבל
spec (מילון תוכן בלבד) ומשכפל לכל תת-סעיף **שקף-תבנית מאוחד אחד** מהספרייה המעודכנת (התבניות
עצמן כבר מכילות את הרמז ותיבות המשוב חונים משמאל, בעיצוב הרשמי) — וממלא טקסטים בלבד: תרחיש
(spacer+בולד-סגול), תשובה/מסיחים+סימון-ירוק/היגדים, רמז, ושני המשובים. אינדקסים מ-`TEMPLATES`
(לא לנחש ידנית), עיגון אוטומטי, ניקוי (הערות "למפתחת:", banner, דמו-אחוז אלא אם `percent_field`).
**הערות-הדובר מועתקות מילה-במילה מהתבנית — אסור להוסיף הנחיות הפקה** (checklist פריט 14;
`notes_append` רק לטקסט הפקה שמופיע מילה-במילה בוורד). ר' docstring לפורמט ה-spec המלא.
דלתת-השקפים הצפויה = מספר הסעיפים.

עקרונות שחלים תמיד (גם אם יורדים לפרימיטיבים במקרה חריג שהבנאי לא מכסה):
1. תוכן חדש **רק** על צורות קיימות ומעוצבות מהתבנית המשוכפלת (`replace_paragraphs`/
   `set_simple_text`) — לעולם לא `add_textbox`/`add_shape` גנרי.
2. להחיל את `content-style-rules.md`: אות סעיף בהנחיה, בולד+סגול, ליטוש עריכתי, מינוח קבוע.
3. משוב שלילי: לבחור פתיח מ-`feedback-bank.md` (לא לחזור על אותו אחד בין שאלות סמוכות).
4. אם רלוונטי: לשלב שאלת פיגום מ-`metacognition-bank.md`.
5. תבנית שלא ב-`TEMPLATES` — לאמת את האינדקסים שלה מול `תבנית לתסריט.pptx` בפועל ולהוסיף אותה
   לרישום, לא לכתוב אינדקסים inline בסקריפט.

### שלב 5 — אימות
**דרך המלך: `pptx_slide_ops.verify_question_build(path, section_name, before_count, expected_delta)`**
— אחרי שמירה, קריאה אחת שמחזירה report עם `ok=True` רק אם: zip תקין, דלתת-שקפים נכונה, המקטע
קיים עם השקפים הנכונים, ו-residue ריק (hits של "מקום לגיף" — מוסכמה מכוונת — מדווחים בנפרד,
לא כ-residue). **חובה על הקובץ אחרי שמירה, לא על אובייקט בזיכרון.**
בנוסף, ידנית: קריאה חוזרת של הטקסט שנכתב (ספוט-צ'ק תוכן — תשובות נכונות, חישובים במשוב),
ובדיקת גלישת טקסט בקונטיינרים עם תוכן צפוף מהרגיל.

### שלב 6 — הצגה למשתמש
לסכם: מה נבנה (שקפים/מקטע), אילו החלטות עיצוב/תוכן התקבלו תוך כדי (ליטוש נוסח, בחירת תבנית אם
היה יותר מאפשרות סבירה אחת), ומה כדאי לבדוק ויזואלית ב-PowerPoint. **לא סיום סופי** — לצפות לסבב
תיקונים מהמשתמש ולעדכן את הכללים כאן אם מתגלה מוסכמה חדשה.

## כללי בטיחות
- לעבוד **ישירות על קובץ היעד** — לא ליצור `_BACKUP_*` בתיקיית הפרויקט של המשתמש בכל שינוי.
- בדיקת לוגיקה חדשה/מסוכנת (למשל תבנית לא-מוכרת) — אך ורק על עותק בתיקיית ה-scratchpad של הסשן.
- לא לדרוס תוכן קיים בקובץ היעד בלי לוודא תחילה שהמיקום/העוגן נכונים.
